<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.digitalhouse.desafio04dh">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Desafio04DH">
        <activity
            android:name=".view.DetailActivity"
            android:parentActivityName=".view.MainActivity"
            android:theme="@style/Theme.MainScreen" />
        <activity
            android:name=".view.AddGameActivity"
            android:theme="@style/Theme.MainScreen" />
        <activity
            android:name=".view.RegisterActivity"
            android:theme="@style/Theme.RegisterScreen" />
        <activity
            android:name=".view.LoginActivity"
            android:theme="@style/Theme.LoginScreen" />
        <activity
            android:name=".view.SplashActivity"
            android:theme="@style/Theme.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.MainActivity"
            android:label="Procurar jogos"
            android:theme="@style/Theme.MainScreen" />
    </application>

</manifest>